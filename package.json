{
  "name": "blenderwebviewer",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "start": "yarn --cwd ./viewer start ./viewer",
    "dependencies": "yarn --cwd ./viewer install && yarn --cwd ./gltfjsx install"
  }
}
