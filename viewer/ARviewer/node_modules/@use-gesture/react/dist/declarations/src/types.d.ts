/// <reference types="react" />
import { DOMHandlers } from '@use-gesture/core/types';
export declare type ReactDOMAttributes = {
    [Key in keyof DOMHandlers]: React.DOMAttributes<EventTarget>[Key];
};
